import {MorningReport,OperationsStatus} from "@/lib/types";
export default function OvernightReport({report,operations}:{report:MorningReport|null;operations:OperationsStatus|null}){if(!report)return null;return <section className="overnight-report"><div><p className="eyebrow">OVERNIGHT REPORT</p><h2>{report.jobs_discovered} jobs discovered</h2><p>{report.applications_submitted} submitted · {report.applications_needing_review} need attention · {report.captchas} CAPTCHA blocks</p></div><div><b>${report.estimated_openai_cost.toFixed(2)}</b><span>estimated AI cost</span></div><div className="runtime-lights"><span>WORKER <b className={operations?.heartbeats.worker?.status==="ONLINE"?"online":"offline"}>{operations?.heartbeats.worker?.status??"OFFLINE"}</b></span><span>SCHEDULER <b className={operations?.heartbeats.scheduler?.status==="ONLINE"?"online":"offline"}>{operations?.heartbeats.scheduler?.status??"OFFLINE"}</b></span></div></section>}

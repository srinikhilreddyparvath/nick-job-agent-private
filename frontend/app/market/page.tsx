import {getJobs} from "@/lib/server-api";
function counts(values:(string|null)[]){return [...values.reduce((m,x)=>{if(x)m.set(x,(m.get(x)??0)+1);return m},new Map<string,number>())].sort((a,b)=>b[1]-a[1])}
export default async function Market(){const jobs=(await getJobs()).items;const roles=counts(jobs.map(x=>x.role_family));const locations=counts(jobs.map(x=>x.normalized_location??x.location));const skills=counts(jobs.flatMap(x=>x.missing_skills).map(x=>x));const salaries=jobs.filter(x=>x.salary_min||x.salary_max);return <div className="page-wrap"><header className="topbar"><div><p className="eyebrow">LOCAL DATASET INSIGHTS</p><h1>Market intelligence</h1><p>Patterns calculated only from jobs stored in your own database.</p></div></header><div className="insight-grid"><section className="panel"><h2>Role families</h2>{roles.map(([x,n])=><p className="insight-row" key={x}><span>{x.replaceAll("_"," ")}</span><b>{n}</b></p>)}</section><section className="panel"><h2>Locations</h2>{locations.map(([x,n])=><p className="insight-row" key={x}><span>{x}</span><b>{n}</b></p>)}</section><section className="panel"><h2>Requested skill patterns</h2>{skills.length?skills.slice(0,8).map(([x,n])=><p className="insight-row" key={x}><span>{x}</span><b>{n}</b></p>):<p>Not enough structured skill data yet.</p>}</section><section className="panel"><h2>Compensation visibility</h2><strong className="big-stat">{jobs.length?Math.round(salaries.length/jobs.length*100):0}%</strong><p>of stored roles include a salary bound.</p></section></div><p className="data-note">Small samples are descriptive only. The agent does not infer market-wide conclusions from sparse local data.</p></div>}
